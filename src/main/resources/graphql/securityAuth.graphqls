type JwtUser {
    id: ID!
    userName: String!
    email: String!
    password: String!
}

extend type Query {
    getAuthenticationToken(userName:String!):JwtUser
}

extend type Mutation {
    createUser(userName: String!,email: String!,password: String!): JwtUser
}